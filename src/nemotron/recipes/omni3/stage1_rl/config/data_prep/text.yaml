stage: text
dataset_name: text_only_rl_stage1
source_uri: hf://nvidia/Nemotron-3-Nano-RL-Training-Blend
source_revision: ffd169f2b74bb492ec607d64bd56f7435054972b
# Reuses Nano3's existing recipes.rl.run_rl_resolve_pipeline. The blend below
# is identical in shape to nano3/stage2_rl/config/data_prep/data_blend_raw.json
# because the omni3 text RL stage consumes the same dataset with the same
# placeholder semantics (DAPO + Skywork resolution).
blend_path: ${oc.env:PWD}/src/nemotron/recipes/omni3/stage1_rl/config/data_prep/data_blend_raw.json
output_dir: ${oc.env:NEMO_RUN_DIR,.}/stage1_rl/data_prep/text
sample: null
force: false
execution_mode: auto
