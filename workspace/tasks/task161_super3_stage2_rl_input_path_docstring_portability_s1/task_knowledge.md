# task161_super3_stage2_rl_input_path_docstring_portability_s1 knowledge

<!-- METADATA:SESSION=1 -->

## Working Notes

- The Stage2 RL portable `input_path` docstring example is
  `${NEMO_RUN_DIR:-.}/output/super3/stage2_rl/rlvr1.jsonl`.
- This task is docstring/static-test only; `SubStageDataPrepConfig` runtime
  defaults and Stage2 RL YAML files are intentionally unchanged.
